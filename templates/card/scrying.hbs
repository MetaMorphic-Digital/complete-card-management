<fieldset class="cards">
  {{#each cards as |card index|}}
  <figure data-card-id="{{card.id}}" data-index="{{index}}">
    <img class="thumbnail" src="{{card.img}}">
    <figcaption>{{card.name}}</figcaption>
  </figure>
  {{/each}}
</fieldset>
