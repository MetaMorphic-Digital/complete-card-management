
/**
 * Creates a grid of placed cards
 * @param {Cards} cardStack - The Cards document to draw from
 * @param {number} rows     - Number of rows to layout
 * @param {number} columns  - Number of columns to layout
 * @param {object} options  - Options modifying the layout
 */
export function grid(cardStack, rows, columns, options = {}) {}
