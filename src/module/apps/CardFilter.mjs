
/**
 * An advanced filter dialog for the Cards placeable tab.
 * @extends {PlaceableFilter}
 */
export default class CardFilter extends foundry.applications.sidebar.filters.PlaceableFilter {
  /** @inheritdoc */
  static DEFAULT_OPTIONS = {
    id: "card-filter",
  };
}
